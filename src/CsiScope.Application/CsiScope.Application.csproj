<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <Description>CsiScope application layer — orchestrates the domain via ports; no hardware or UI knowledge.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\CsiScope.Domain\CsiScope.Domain.csproj" />
  </ItemGroup>

</Project>
